{
  "name": "fit-rapido-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd admin && npm install && npm run build",
    "postbuild": "cp -r admin/.next . && cp -r admin/public . && cp admin/package.json . || echo 'Ambiente Windows ou erro na copia'",
    "start": "next start",
    "install:admin": "cd admin && npm install",
    "install:api": "cd api && npm install"
  },
  "dependencies": {
    "next": "latest"
  }
}
